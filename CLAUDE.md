# CLAUDE.md

This repo is a single static redirect page deployed to GitHub Pages via `.github/workflows/deploy.yml` on every push to `main`.

`index.html` redirects to `https://sharatvisweswara.blog` using `<meta http-equiv="refresh">`.
